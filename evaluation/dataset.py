# This file is generated by Claude
'''
prompt for generating dataset:

I have uploaded `chunks.jsonl`, which contains the complete set of text chunks from my RAG system. Each chunk has the following structure:

```json
{
  "chunk_id": "...",
  "chunk_content": "..."
}
```

Using **only the information contained in these chunks**, create a high-quality evaluation dataset for my RAG system.

### Dataset Requirements

Generate **20–30 unique, non-repetitive evaluation questions**.

The questions must:

1. Be directly answerable from the provided chunks.
2. Be grounded strictly in the chunk content — **do not introduce outside knowledge or assumptions**.
3. Cover different parts of the codebase/project rather than repeatedly asking about the same functionality.
4. Test different levels of understanding, including:

   * Direct factual retrieval
   * Understanding how components work
   * Relationships between components
   * Data/control flow
   * Design or implementation decisions explicitly described in the chunks
   * Multi-chunk questions that require combining information from multiple chunks
5. Prefer realistic questions that a developer would actually ask when trying to understand or use this codebase.
6. Avoid trivial questions such as "What is this file called?" unless the filename itself is important to understanding the system.
7. Avoid questions whose answers can be inferred without retrieving the relevant chunks.
8. Avoid duplicate or near-duplicate questions.
9. Include a reasonable mixture of:

   * **Single-chunk questions**
   * **Multi-chunk questions**
   * Questions requiring retrieval of related information from different parts of the codebase.

### Ground Truth Requirements

For every question, provide:

* `id`: Sequential identifier such as `q001`, `q002`, etc.
* `question`: The evaluation question.
* `relevant_chunk_ids`: The **exact `chunk_id` values** of all chunks required to answer the question correctly.
* `reference_answer`: A concise but complete answer derived only from the relevant chunks.

### Important Rule for `relevant_chunk_ids`

Be precise.

Only include a chunk ID if that chunk contains information that is genuinely necessary to answer the question.

For a question that can be answered completely from one chunk:

```json
"relevant_chunk_ids": ["chunk_123"]
```

For a question requiring information from multiple chunks:

```json
"relevant_chunk_ids": ["chunk_123", "chunk_456", "chunk_789"]
```

Do **not** mark a chunk as relevant merely because it is related to the topic. The IDs will be used as ground truth to calculate retrieval metrics such as **Recall@K, Precision@K, MRR, and NDCG@K**, so incorrect labels will directly affect the validity of the evaluation.

### Reference Answer Requirements

Reference answers should:

* Answer the question directly.
* Contain enough detail to establish correctness.
* Use only information available in the provided chunks.
* Not include information from your general knowledge.
* Not mention that the answer came from a chunk.
* Avoid unnecessary verbosity.
* Correctly combine information when multiple chunks are required.

### Coverage Requirements

Before generating the final dataset, analyze the entire `chunks.jsonl` and identify the major functional areas represented in the chunks.

Distribute the questions across these areas so that the dataset does not disproportionately test one component.

Where applicable, cover areas such as:

* Document ingestion
* Document loading
* Chunking
* Chunk IDs / metadata
* Embedding generation
* Vector-store interaction
* ChromaDB configuration
* Query processing
* Retrieval
* Ranking / similarity search
* Context construction
* LLM generation
* Application flow
* Configuration
* Error handling
* Any other significant functionality actually present in the chunks

Do **not** force questions into categories that are not represented in the provided chunks.

### Quality-Control Pass

Before returning the dataset, perform a validation pass:

1. Verify that every question is answerable using only the provided chunks.
2. Verify that every `relevant_chunk_id` exists in `chunks.jsonl`.
3. Verify that every relevant chunk is genuinely necessary for the reference answer.
4. Verify that the reference answer is supported by the listed chunks.
5. Remove duplicate or highly similar questions.
6. Ensure the questions collectively cover different parts of the codebase.
7. Ensure there are both single-hop and multi-hop retrieval questions.
8. Ensure no outside knowledge has been introduced.

### Output Format

Return **only valid JSON**.

The output must be a JSON array with no Markdown, comments, explanations, or code fences.

Example:

[
{
"id": "q001",
"question": "How is JWT authentication implemented?",
"relevant_chunk_ids": [
"a31f92c...",
"8d21ab..."
],
"reference_answer": "JWT authentication is implemented by..."
},
{
"id": "q002",
"question": "How are routes registered in the backend?",
"relevant_chunk_ids": [
"72ac91..."
],
"reference_answer": "Routes are registered through..."
}
]

The final dataset should contain **20–30 high-quality questions**, with no unnecessary repetition and with accurate ground-truth chunk IDs.

'''



import json

dataset = [
{
"id": "q001",
"question": "According to the project's schema design document, what fields make up the Users collection?",
"relevant_chunk_ids": ["1c26c48984b6e44b"],
"reference_answer": "The Users collection has: _id, userName, email (unique, indexed), password (hashed), pfp (profile image), role (enum: rider | organizer | sponsor), createdAt, and updatedAt."
},
{
"id": "q002",
"question": "What does the Riders <-> Rides join collection track in the data model?",
"relevant_chunk_ids": ["d76c2638a7ea4306"],
"reference_answer": "It tracks _id, riderId (ObjectId), rideId (ObjectId), status, and purchasedAt — linking a rider to a ride they have booked."
},
{
"id": "q003",
"question": "Does the communicationType enum implemented in the Ride Mongoose schema match what the original design document specified?",
"relevant_chunk_ids": ["d76c2638a7ea4306", "d8ed6db1861bb3f1"],
"reference_answer": "No. The design document specifies communicationType as [telegram, whatsapp, email], but the implemented Ride schema's communicationType enum is ['telegram', 'whatsapp', 'instagram'] — email was dropped and instagram was added instead."
},
{
"id": "q004",
"question": "What information is required on the login page?",
"relevant_chunk_ids": ["ea014fee42390eb6"],
"reference_answer": "The login page requires a User-Name/Email (a unique identifier) and the account Password."
},
{
"id": "q005",
"question": "What basic (role-independent) information is collected on the signup page before role-specific details are asked for?",
"relevant_chunk_ids": ["ea014fee42390eb6"],
"reference_answer": "userName (the account name), email (must be unique), password (hashed before storage), pfp (a profile picture), and role (enum: Rider | Organizer | Sponsor)."
},
{
"id": "q006",
"question": "What role-specific fields does a rider provide during signup, and what are the bikes and ridingStyle fields used for?",
"relevant_chunk_ids": ["43bd6a9076a559f4"],
"reference_answer": "A rider provides fName, mName (if applicable), lName, age, city, banner, socialLinks, bikes, ridingStyle, and bio (max 250 characters). bikes is an array of bikes the rider owns, selected from a dropdown, and is used to suggest rides to them; ridingStyle is an array of the rider's preferred riding styles (e.g. cruising, off-roading, adventure, highway)."
},
{
"id": "q007",
"question": "What is the GSTIN field used for during organizer signup?",
"relevant_chunk_ids": ["d91f37691fadcafc"],
"reference_answer": "GSTIN is the organizer's GSTIN number, used to verify the organization."
},
{
"id": "q008",
"question": "Does the actual Rider Mongoose schema store the same name and image fields that the signup requirements document describes for riders?",
"relevant_chunk_ids": ["43bd6a9076a559f4", "ecadc52ca4d83029"],
"reference_answer": "No. The requirements document describes separate fName, mName, and lName fields plus a banner image for riders, but the implemented riderSchema only stores a single \"name\" String field and has no banner field. The schema does store age, city, a cityLocation GeoJSON point, socialLinks, bikes, ridingStyle, and bio, matching the rest of the requirements."
},
{
"id": "q009",
"question": "What fields does the Organizer Mongoose model (org.model.js) define, and which field must be unique?",
"relevant_chunk_ids": ["f67c8c8d7f2f6d0e"],
"reference_answer": "The orgSchema defines userId (ObjectId, unique), name, description, socialLinks, website, and GSTIN, stored in the 'Organizers' collection. userId is the field marked unique."
},
{
"id": "q010",
"question": "How is a ride's location represented in the Ride schema, and what database index supports location-based queries on it?",
"relevant_chunk_ids": ["160ab712ea43e180", "4c20bcece8af698d"],
"reference_answer": "startLocation and endLocation each use a shared locationSchema shaped as GeoJSON: { type: 'Point', coordinates: [longitude, latitude] }. The rideSchema creates a 2dsphere index on startLocation (rideSchema.index({ startLocation: \"2dsphere\" })) to support geospatial queries, and the model is registered as 'Rides'."
},
{
"id": "q011",
"question": "What is the default value assigned to a new user's profile picture (pfp) in the User model?",
"relevant_chunk_ids": ["18cce76253f9ccbb"],
"reference_answer": "pfp defaults to `${process.env.R2_PUBLIC_URL}/pfp/default_pfp.png`."
},
{
"id": "q012",
"question": "Walk through what happens when a rider signs up with a profile picture: from location validation through to the database transaction being committed.",
"relevant_chunk_ids": ["0876deb032c60fa2", "2ca078cae234008d", "a70d77eff1c27269", "5ad59e04e860d1b4"],
"reference_answer": "sign_up_user connects to the DB and starts a Mongo session/transaction, validates the role is one of rider/organizer/sponsor, hashes the password with bcrypt, and saves a new User document. For a rider, insert_role_based checks that lat and lng were provided and are valid numbers, throwing \"No Location Entered\" if not, then saves a Rider document whose cityLocation is a GeoJSON Point built from those coordinates. If an image file was supplied, it is uploaded to R2 under a pfp/<userId>.<ext> key and the user's pfp field is updated to the public R2 URL. The transaction is then committed and the new user's data (including pfp) is returned."
},
{
"id": "q013",
"question": "What safeguard exists if the profile picture upload to R2 succeeds but a later step in signup fails?",
"relevant_chunk_ids": ["5ad59e04e860d1b4"],
"reference_answer": "The catch block checks a r2Uploaded flag; if true, it deletes the just-uploaded R2 object with a DeleteObjectCommand before aborting the Mongo transaction and rethrowing the error, preventing an orphaned image in storage."
},
{
"id": "q014",
"question": "How is Google Sign-In validated on the backend, and what happens if no matching user exists?",
"relevant_chunk_ids": ["7ffa47c6bbeadc7f"],
"reference_answer": "google_sign_in_user uses google-auth-library's OAuth2Client.verifyIdToken with the received credential and the CLIENT_ID as audience, extracts the email from the token payload, and looks up a User by that email. If no user is found, it returns { success: false, message: \"User Invalid, please sign up\" }; otherwise it returns the user's _id, username, email, role, and pfp."
},
{
"id": "q015",
"question": "How is a user's password verified during login?",
"relevant_chunk_ids": ["411531d11a3e3124"],
"reference_answer": "login_user compares the submitted password against the stored hash using bcrypt.compare(data.password, hashedPass). If it matches, it returns the user's _id, username, email, role, and pfp with success true; if not, it returns success false with the message \"Entered Password is incorrect\"."
},
{
"id": "q016",
"question": "How is a JWT created after login, and how is it delivered back to the client?",
"relevant_chunk_ids": ["749f21d5b514f644", "532327cbc55f17de"],
"reference_answer": "create_jwt_token signs the user payload with jwt.sign using process.env.SECRET_KEY_JWT and an expiry. In the login controller, this token is set as a cookie named 'token' with httpOnly: true, sameSite: 'none', secure: true, and a maxAge of 7 days, alongside a JSON response containing the user's id, username, email, role, and pfp."
},
{
"id": "q017",
"question": "What does authMiddleware do when a request has no token cookie, versus when the token is present but invalid?",
"relevant_chunk_ids": ["3d33979a2da6235f"],
"reference_answer": "If req.cookies.token is missing, it responds with 400 and \"No Token Found\". If a token is present but jwt.verify throws (invalid/expired), it responds with 401 and \"Invalid token\". If verification succeeds, it sets req.user to the decoded payload and calls next()."
},
{
"id": "q018",
"question": "How does the authorizeRole middleware restrict access to a route?",
"relevant_chunk_ids": ["cd5c6067a4ce59f7"],
"reference_answer": "authorizeRole(role) returns a middleware that compares req.user.role to the required role; if they don't match it responds with 403 and \"User not authorized\", otherwise it calls next()."
},
{
"id": "q019",
"question": "What size limit is enforced on the signup profile picture, and where in the request flow is it checked?",
"relevant_chunk_ids": ["07bfbd5f2ec181c6"],
"reference_answer": "In the signup controller, before calling the signup service, it checks if imgFile.size exceeds 2MB (2 * 1024 * 1024 bytes); if so it responds with 400 and \"Please upload the image within 2MB\"."
},
{
"id": "q020",
"question": "Which storage service is used for uploaded images, and what environment variables configure the client?",
"relevant_chunk_ids": ["11daf76951448b21"],
"reference_answer": "Images are stored in Cloudflare R2, accessed via the AWS S3Client. The client is configured with region 'auto', an endpoint built from process.env.R2_ACCOUNT_ID, and credentials from process.env.R2_ACCESS_KEY_ID and process.env.R2_SECRET_ACCESS_KEY."
},
{
"id": "q021",
"question": "When an organizer creates a ride with a banner image, how are the location fields and the image handled before the ride is saved?",
"relevant_chunk_ids": ["0c8e863c07449d7c"],
"reference_answer": "The create_ride controller JSON.parses data.startLocation and data.endLocation from the request body, sets data.userId from req.user._id, and if a file was uploaded, uploads it to R2 under a key like rides/<timestamp>-<originalname> and sets data.banner to the resulting public R2 URL, before passing data to rideService.create_ride."
},
{
"id": "q022",
"question": "How does the get_rides service query for upcoming rides near a user, and how many results does it return?",
"relevant_chunk_ids": ["ca7ad608410da0fe", "1618694e6d8ec673"],
"reference_answer": "get_rides runs a Ride.aggregate pipeline using $geoNear centered on the user's coordinates, filtered to status: 'Upcoming', capped with a $limit of 20, and projecting _id, title, description, banner, difficulty, and type. If no rides are found it returns { success: false, message: \"No Rides available\" }, otherwise it returns the matched rides."
},
{
"id": "q023",
"question": "How does get_rides_type_based's query differ from the plain get_rides query?",
"relevant_chunk_ids": ["ca7ad608410da0fe", "1618694e6d8ec673", "7e4f5456931d0487"],
"reference_answer": "get_rides has no ride-type filter and returns results unsorted after the $geoNear/$limit/$project stages. get_rides_type_based adds a type: type condition to the $geoNear query (alongside status: 'Upcoming') and appends a $sort stage ordering results by startDate ascending."
},
{
"id": "q024",
"question": "In the ride_details service, how is a rider's ability to book a ride (canBook) determined?",
"relevant_chunk_ids": ["93bc6336c238886e"],
"reference_answer": "canBook is false if bookedSeats is greater than or equal to maxSeats (the ride is full). If the ride is brand-locked (ride.crossBike === false), canBook is true only if the rider's bikes array includes the ride's brand, otherwise false. If cross-bike riding is allowed (or the field isn't set) and seats are available, canBook is true."
},
{
"id": "q025",
"question": "How does ride_details determine canEdit for an organizer and canContactForSponsor for a sponsor viewing a ride?",
"relevant_chunk_ids": ["57b6cc8a4d400183"],
"reference_answer": "canEdit is set to true only when the ride's userId matches the requesting organizer's _id (comparing them as strings); otherwise it is false. canContactForSponsor is simply set to true for any sponsor viewing the ride."
},
{
"id": "q026",
"question": "How does the organizer dashboard's basic analysis endpoint compute totalBookingsDone and revenueGenerated?",
"relevant_chunk_ids": ["e29263479a6d01c8", "5849798aebb86766"],
"reference_answer": "get_basic_analysis fetches all Ride documents whose userId matches the organizer's _id. ridesCreated is the count of those rides. It then loops over the rides, summing each ride's bookedSeats into totalBookingsDone and bookedSeats * entryFee into revenueGenerated, returning { ridesCreated, totalBookingsDone, revenueGenerated }. If the organizer has no rides, it returns { success: false, message: \"No Rides from this user\" }."
},
{
"id": "q027",
"question": "How does the dashboard's successful-rides endpoint rank an organizer's rides?",
"relevant_chunk_ids": ["f8d21eb66ec60f98"],
"reference_answer": "It aggregates the organizer's rides, adds a computed occupancyRate field (bookedSeats divided by maxSeats, or 0 if maxSeats is 0), sorts the results in descending order of occupancyRate, and projects _id, title, bookedSeats, entryFee, startAddress, and endAddress."
},
{
"id": "q028",
"question": "What does the organizer dashboard show in the successful-rides table when the fetch fails versus when there are simply no successful rides yet?",
"relevant_chunk_ids": ["72af3c316f4e221d"],
"reference_answer": "If the response's success flag is false, the table body shows an empty-state row with a warning icon and the text \"Could not load rides\". If the request succeeds but the rides array is empty or missing, it instead shows an empty-state row with a diamond icon and the text \"No successful rides yet\"."
},
{
"id": "q029",
"question": "How does the organizer's rides grid choose a difficulty badge class and a fallback banner image for each ride card?",
"relevant_chunk_ids": ["569283080c3505e4", "f9671a09a57ab79f"],
"reference_answer": "difficultyClass() lowercases the ride's difficulty and maps it to one of 'difficulty-easy', 'difficulty-moderate', 'difficulty-hard', or 'difficulty-extreme', defaulting to 'difficulty-moderate' if the value doesn't match. For the image, each card uses ride.banner if present, otherwise a hardcoded Unsplash URL, which is also wired as the img's onerror fallback; the difficulty label text defaults to 'Moderate' if ride.difficulty is missing."
},
{
"id": "q030",
"question": "How does the city search autocomplete work when creating a ride or signing up?",
"relevant_chunk_ids": ["c97cf4c7cf5c76c2", "da1412a00e3ba879"],
"reference_answer": "As the user types, input is debounced for 380ms and only triggers a search once the query is at least 2 characters; if the user edits the field after already picking a city, the prior selection is cleared. fetchCities() then queries the Nominatim OpenStreetMap search API (format=json, limit=6, addressdetails=1) with 'Accept-Language' and a custom 'User-Agent' header, showing a spinner while the request is in flight, and renders the returned results as suggestions (or a \"No locations found\" message)."
},
{
"id": "q031",
"question": "How does the ride details page's action footer differ across the rider, organizer, and sponsor roles?",
"relevant_chunk_ids": ["ae347b515dfed2d4", "1c362d3d9bbf88bd", "6c5381778422b9f7", "8411e837d7dc908a"],
"reference_answer": "For a rider, it shows a \"Book this ride\" button, enabled only if flags.canBook is true; if not bookable, it shows a disabled button plus a headline/sub explaining whether the ride is full, the rider's bike doesn't match a brand-locked ride, or booking is otherwise unavailable. For an organizer, if flags.canEdit is true it shows \"You organized this ride\" with \"Cancel ride\" and \"Edit ride\" buttons; otherwise it shows a read-only message that only the creating organizer can edit it. For a sponsor, it shows a \"Contact organizer\" button, disabled only when flags.canContactForSponsor is explicitly false."
}
]

with open('dataset.json', 'w') as f:
    json.dump(dataset, f, indent=2, ensure_ascii=False)

print(len(dataset), 'questions written')